import React from 'react'
import styles from './GameWord.module.css';

function gameWord({ word, def }) {
  return (
    <div className={styles.gameWord}>
      <h3>dfg</h3>
    </div>
  )
}

export default gameWord
